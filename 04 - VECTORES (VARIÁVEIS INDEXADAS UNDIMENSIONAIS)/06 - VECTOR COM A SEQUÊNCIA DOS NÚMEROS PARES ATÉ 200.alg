/*
 * Fazer um algoritmo que preenche
 * um vector com a sequência de números
 * pares até 200.
 */

 inicio
 	inteiro A[200], i, k <- 0
 	para i de 0 ate 199
 		A[i] <- k
 		k <- k + 2
 		escreva "[", A[i], "]\n"
 	fimpara
fimalgoritmo
 	